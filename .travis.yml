dist: xenial
language: c++
sudo: false

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.9
    - iverilog
    - llvm

before_install:
  - dpkg -L libz3
  - cmake .
  - make -j

script:
  - ./all-tests

compiler:
  - gcc

os:
- linux