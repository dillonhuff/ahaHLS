dist: xenial
language: c++
sudo: false

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.9
    - iverilog
    - llvm

before_install:
  - cmake .
  - make -j

script:
  - dpkg -L libz3
  - ./all-tests

compiler:
  - gcc

os:
- linux